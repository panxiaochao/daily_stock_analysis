# 01-项目概述

## 项目定位

股票智能分析系统（Daily Stock Analysis, DSA），基于 AI 大模型实现 A股/港股/美股自选股的自动化分析，每日自动生成「决策仪表盘」并推送至企业微信/飞书/Telegram/Discord/Slack/邮箱等渠道。

## 核心能力

| 能力 | 覆盖内容 |
|------|---------|
| AI 决策报告 | 核心结论、评分、趋势、买卖点位、风险警报、催化因素、操作检查清单 |
| 多市场数据聚合 | A股、港股、美股、ETF；行情、K线、技术指标、资金流、筹码、新闻、公告和基本面 |
| Web/桌面工作台 | 手动分析、任务进度、历史报告、完整Markdown、回测、持仓、配置管理、浅色/深色主题 |
| Agent 策略问股 | 多轮追问，支持均线、缠论、波浪、趋势等 11 种内置策略，覆盖 Web/Bot/API |
| 智能导入与补全 | 图片、CSV/Excel、剪贴板导入；股票代码/名称/拼音/别名补全 |
| 自动化与推送 | GitHub Actions、Docker、本地定时任务、FastAPI 服务和企业微信/飞书/Telegram/Discord/Slack/邮件推送 |

## 技术栈

### 后端
- Python 3.10+
- FastAPI + Uvicorn（Web 服务）
- SQLAlchemy（数据库 ORM）
- LiteLLM（统一 LLM 调用）

### 数据源
- A股：东方财富(eFinance)、AkShare、Tushare、通达信(Pytdx)、Baostock
- 港股/美股：YFinance、Longbridge、TickFlow
- 新闻：Anspire、SerpAPI、Tavily、Bocha、Brave、MiniMax、SearXNG

### 前端
- Web：React + TypeScript + Vite
- 桌面端：Electron
- UI：shadcn/ui

### 部署
- GitHub Actions（推荐）
- Docker
- 本地/云服务器

## 目录结构

```
daily_stock_analysis/
├── main.py                 # CLI 主入口
├── server.py               # FastAPI 服务入口
├── requirements.txt        # Python 依赖
│
├── src/                    # 核心业务代码
│   ├── core/               # 核心编排层
│   │   ├── pipeline.py     # 分析流水线
│   │   ├── market_review.py # 大盘复盘
│   │   ├── trading_calendar.py # 交易日历
│   │   └── backtest_engine.py  # 回测引擎
│   ├── services/           # 业务服务层
│   ├── agent/              # Agent 策略系统
│   ├── repositories/       # 数据访问层
│   ├── schemas/            # 数据结构定义
│   └── ...
│
├── data_provider/          # 多数据源适配层
│   ├── efinance_fetcher.py
│   ├── akshare_fetcher.py
│   ├── tushare_fetcher.py
│   └── ...
│
├── api/                    # FastAPI REST API
│   └── v1/endpoints/
│
├── bot/                    # 机器人平台集成
│   ├── platforms/          # 企微/飞书/钉钉/Discord/Slack
│   └── commands/           # 命令处理
│
├── apps/
│   ├── dsa-web/            # React Web 应用
│   └── dsa-desktop/        # Electron 桌面应用
│
├── scripts/                # 本地脚本
├── docker/                 # Docker 配置
├── docs/                   # 项目文档
├── tests/                  # pytest 测试
└── .github/workflows/      # CI/CD 流水线
```

## 主要入口

| 入口 | 说明 |
|------|------|
| `python main.py` | 分析任务主入口 |
| `python main.py --serve` | 启动 FastAPI 服务并执行分析 |
| `python main.py --serve-only` | 仅启动 FastAPI 服务 |
| `python main.py --schedule` | 定时任务模式 |
| `python main.py --webui` | 启动 Web 管理界面 |
| `python main.py --market-review` | 仅运行大盘复盘 |
| `python main.py --backtest` | 运行回测 |

## 相关项目

| 项目 | 定位 |
|------|------|
| [AlphaSift](https://github.com/ZhuLinsen/alphasift) | 多因子选股与全市场扫描 |
| [AlphaEvo](https://github.com/ZhuLinsen/alphaevo) | 策略回测与自我进化 |

## 协议

MIT License